{
  "name": "waterlens-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "node build.js",
    "dev": "npm run dev --workspace=frontend",
    "preview": "npm run preview --workspace=frontend"
  }
}
